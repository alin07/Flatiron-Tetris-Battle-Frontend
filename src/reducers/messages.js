// import { ADD_MESSAGE } from '../actions/types'

let msgId = 0

const initState = {
  messages: []
}

const messages = (state = initState, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default messages
